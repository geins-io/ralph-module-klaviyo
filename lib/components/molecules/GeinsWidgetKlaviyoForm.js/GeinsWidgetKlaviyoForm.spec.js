import { shallowMount } from '@vue/test-utils';
import GeinsWidgetKlaviyoForm from './GeinsWidgetKlaviyoForm.vue';

describe('GeinsWidgetKlaviyoForm.vue', () => {
  it('renders a component', () => {
    const component = shallowMount(GeinsWidgetKlaviyoForm);
  });
});
